#ifndef COMPILER
#define COMPILER

/*
Defines the prototypes for the compilation chain
*/

#include "Settings.hpp"
#include "Registry.hpp"

//load project file
bool loadCNS(settings&, fileRegistry&);

#endif
