#ifndef COMPILER
#define COMPILER

#include "Collections.hpp"

#include <string>
#include <vector>

using namespace std;

int compileToObject(settings&,project&);
int compileToExec(settings&,project&);

#endif
